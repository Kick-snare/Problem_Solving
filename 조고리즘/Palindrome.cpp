#include <string>
#include <iostream>
using namespace std;

int n;

int is_parlindrome(string str, bool is_rsb){
    for(int i=0;i<str.length();i++){
        if()
    }
}

int main(){
    string tmp;
    cin >> n;
    for(int i=0;i<n;i++){
        cin >> tmp;
        int result = is_parlindrome(tmp,true);
        if(result==1) cout << '1';
        else if(result==2) cout << '2';
        else cout << '3'
    }


    return 0;
}